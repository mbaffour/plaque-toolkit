name: Build macOS app

# Build the macOS "Plaque Toolkit.app" (+ .dmg) on real Apple runners, since PyInstaller
# cannot cross-compile from Windows. Runs on demand (Actions tab -> "Run workflow") or when
# you push a version tag like v1.0.0. Download the .dmg from the run's Artifacts section.
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-13   # Intel Macs
            arch: x86_64
          - runner: macos-14   # Apple Silicon (M-series)
            arch: arm64
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          # The app's runtime + build stack (mirrors the Windows 'plaqueapp' env).
          pip install pyinstaller PySide6 opencv-python pillow pillow-heif \
                      pandas imutils matplotlib scikit-image scipy \
                      torch torchvision ultralytics
          # Pin numpy<2 LAST — the validated engine needs numpy 1.x and the --smoke gate
          # asserts it (matches how the Windows env was built).
          pip install "numpy<2"

      - name: Build .app + .dmg (bundles the Precise engine)
        run: |
          chmod +x build_macos.sh
          PLAQUE_PY="$(command -v python)" ./build_macos.sh

      - name: Upload macOS build
        uses: actions/upload-artifact@v4
        with:
          name: PlaqueToolkit-macos-${{ matrix.arch }}
          if-no-files-found: error
          path: |
            dist/*.dmg
            dist/*.app
