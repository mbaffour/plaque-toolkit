@echo off
rem Convenience launcher - runs the real tool in the parent folder so every path resolves.
rem Unified desktop app: Measure + Compare turbidity tabs, sensitive checkbox, dish overlay.
call "%~dp0..\Plaque Toolkit (app).bat" %*
