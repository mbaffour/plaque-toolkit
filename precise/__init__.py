"""Precise pipeline package (PST + PlaqSeg + optional learned classifier).

Exposes the in-process orchestrator so callers can do::

    from precise.pipeline import run_inprocess, torch_inprocess_available
"""
