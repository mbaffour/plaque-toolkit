# Plaque toolset - tested dependency versions (conda env 'plaque', Python 3.9).
# numpy MUST stay < 2: the upstream tool used np.warnings, which numpy 2.0 removed.
numpy<2
opencv-python>=4.6,<5
imutils>=0.5.4
pandas>=1.3,<3
Pillow>=9
matplotlib>=3.5
pillow-heif>=0.13
